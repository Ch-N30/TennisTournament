import SwiftUI

public struct CreateTournamentScreen: View {
    public init() {}

    public var body: some View {
        VStack(spacing: 8) {
            Text("Create Tournament")
                .font(.title2)
            Text("TODO: implement tournament setup flow")
                .font(.subheadline)
                .foregroundStyle(.secondary)
        }
        .navigationTitle("Create")
    }
}
